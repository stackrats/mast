BASE_HOST=example.test
FULL_URL=https://${BASE_HOST}/api
DEFAULTED=${MISSING_VAR:-fallback}
REQUIRED_STYLE=${MUST_EXIST:?is required}
BARE_REF=$BASE_HOST
LITERAL_DOLLARS=pa$$word
DQ_INTERP="host is ${BASE_HOST}"
SQ_NO_INTERP='host is ${BASE_HOST}'
